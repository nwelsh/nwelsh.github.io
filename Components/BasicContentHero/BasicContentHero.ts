import { HeroProps } from '../../Global/Types'
import React, { ReactElement } from 'react'

export function renderHero(props: HeroProps): ReactElement {
    const {image, title, description} = props;


  return (
    <div>
    <p>nicole</p>
    </div>);
}